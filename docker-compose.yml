services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: efference
      POSTGRES_PASSWORD: dev_password
      POSTGRES_DB: efference
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U efference"]
      interval: 10s
      timeout: 5s
      retries: 5

  control_plane:
    build:
      context: ./services/control_plane
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://efference:dev_password@postgres:5432/efference
      MODEL_SERVER_URL: http://model_server:8000/infer
      ADMIN_KEY: dev-admin-key
      LOG_LEVEL: debug
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./services/control_plane/app:/app/app

  model_server:
    build:
      context: ./services/model_server
      dockerfile: Dockerfile
      secrets:
        - github_token
    ports:
      - "8001:8000"
    environment:
      # Model variant: 'd435'
      MODEL_NAME: d435
      MODEL_S3_URI: https://efference-weights.s3.us-west-1.amazonaws.com/public/d435.pth
    volumes:
      - ./services/model_server/app:/app/app
      - model_weights:/app/weights
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped

secrets:
  github_token:
    file: /tmp/github_token

volumes:
  postgres_data:
  model_weights: